# 產品需求規格書（PRD）：山區離網通訊終端 (Cardputer Mesh Terminal)

## 1. 專案概述
本產品旨在打造適用於山區離網（Off-Grid）環境的攜帶式無線通訊與定位終端。透過 **M5Stack Cardputer Adv** 結合 **CAP LoRa-1262 + GNSS** 模組，提供短語音對講、離網文字訊息、GPS 軌跡記錄及隊友相對雷達定位等功能，確保山區隊伍的通訊與安全備援。

---

## 2. 系統架構與對頻機制

### 2.1 群組對頻與安全加密 (Group Pairing & Security)
* **對頻機制**：系統提供「群組對頻」選單，使用者輸入 4 位數群組 PIN 碼（例：`8888`）。
* **頻道自動對應**：系統透過 Hash 演算法，將 PIN 碼自動轉換為 LoRa 的工作頻率頻道（Channel Index）與同步字（SyncWord）。
* **數據加密**：以群組 PIN 碼作為 AES-128 加密密鑰，對所有發射之語音與文字封包進行端到端加密，避免非同組設備竊聽或干擾。

---

## 3. 核心功能規格

### 3.1 語音對講與漏訊偵測
* **按鍵式語音錄製 (PTT)**：
  * 按住 `Space` 鍵：系統發出 200ms 的 1kHz 提示音（嗶一聲），啟動 ES8311 麥克風進行錄音。
  * 放開 `Space` 鍵或到達 5.0 秒上限：自動終止錄音，透過 Codec2 進行語音壓縮並發射。
* **畫面倒數 UI**：錄音時跳出紅色提示框，顯示 5.0 秒倒數計時條（Progress Bar）。
* **封包序列號（Sequence Numbering）**：
  * 每個封包標頭均包含日期與遞增序號（格式：`MMDD-Seq`，例：`0812-0001`）。
  * **漏訊標示**：當接收端收到不連續的序列號時（如收到 `0812-0003` 但缺 `0812-0002`），UI 歷史清單中將以黃字高亮標示 `[漏訊 0812-0002]`。

### 3.2 待機 GPS 軌跡與隊友雷達
* **待機主畫面**：
  * **軌跡記錄 (Track Logger)**：讀取 ATGM336H GPS 數據，於 240x135 螢幕上即時繪製當日行走折線與目前點標記。
* **隊友位置廣播 (Beacon)**：
  * 背景定時器每 10 分鐘自動廣播精簡位置封包（含經緯度、海拔、電量）。
* **相對方位計算**：
  * 系統自動計算隊友之相對距離與大圓方位角（Bearing），並於畫面角落顯示卡片（例：`[隊友 A] 東北 45° / 1.2 km / 高差 +120m`）。

### 3.3 訊息輸入介面
* **進入方式**：點擊鍵盤 `T` 鍵切換至文字模式。
* **發送機制**：
  * **快捷中文選單**：按數字鍵 `1` ~ `9` 選擇預設常用句發送。
    1. 一切平安
    2. 我們在休息用餐
    3. 已登頂
    4. 返回中
    5. 處理狀況中
    6. 請求支援 / 原地等待
  * **英數自由輸入**：支援直接打字輸入英數字串（如 `Need water`, `ETA 1530`）。
  * **SD 卡字庫讀取**：可自 MicroSD 卡讀取 `message.txt` 設定檔，允許使用者自訂快捷短語。


### 3.4 網狀自組網中繼機制 (Mesh Relay Protocol)
* **無中心多跳拓撲**：採用去中心化 Mesh 架構，任意節點均可扮演訊號中繼站（Relay Node）。
* **TTL 轉發限制**：所有封包標頭帶有 `TTL = 2` 設定，限制最大中繼跳數為 2 跳，避免廣播風暴。
* **衝突避讓 (CSMA/CA)**：轉發前自動進行 CAD (Channel Activity Detection) 頻道監聽，若頻道佔用則產生 50~200ms 隨機延遲後再行發射。
* **分級中繼**：
  * **GPS 座標與文字**：允許 2 跳中繼，確保全隊位置同步。
  * **5 秒語音封包**：僅允許 1 跳中繼，以優先保障空中頻寬順暢。

---

## 4. 通訊封包結構 (Data Packet Definition)

整體封包長度控制在 256 Bytes 以內，以確保高 SF（擴頻因子）環境下的傳送成功率：

```cpp
#pragma pack(push, 1)
typedef struct {
    uint32_t group_id;      // 群組 Hash ID (4 Bytes)
    uint8_t  msg_type;      // 0x01:語音, 0x02:快捷/英數文字, 0x03:GPS Beacon
    char     seq_num[10];   // 封包序號 (例 "0812-0001", 10 Bytes)

    // GPS 與系統狀態
    float    latitude;      // 緯度 (4 Bytes)
    float    longitude;     // 經度 (4 Bytes)
    uint16_t altitude;      // 海拔高度 (2 Bytes)
    uint8_t  battery_pct;   // 電量百分比 (1 Byte)

    // Payload 數據
    uint8_t  payload_len;   // Payload 長度
    uint8_t  payload[220];  // Codec2 語音壓縮數據或文字字串
    uint16_t crc16;         // CRC16 校驗碼
} LoRaPacket;
#pragma pack(pop)
